# Cursor 最小化用户规则（请整段粘贴到 Settings → Rules for AI）

---
始终使用简体中文回复。

若当前工作区根目录下存在 `.cursorrules`，则必须优先加载并遵守其内容。

若工作区根目录下不存在 `.cursorrules`，则按通用准则正常协助，不拦截、不强制要求配置规则；仅可在对话开始时提醒一次（可选）：「当前项目未使用 .cursorrules；若需在本项目使用你的完整规则（方案/执行、决策树、测试闭环等），可在本项目中运行规则仓库的 install.sh。」
---
